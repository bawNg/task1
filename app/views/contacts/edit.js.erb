
$('#contacts').html("<%= escape_javascript(render('pagination')) %>");




for each( input in $("input:checkbox")){
    for each(cid in cids_array){
        if(input.value == cid){
            input.checked = true;
            $('#currentpage_contactcount').val(Number($('#currentpage_contactcount')[0].value)+1);
        }
    }
}
